---
- name: wrapper playbook for kitchen testing of the cis setup
  hosts: all
  become: yes
  user: provisioner
  tags:
    - level-2
  roles:
    - { role: ami_cis_base, when: ansible_distribution=="Ubuntu" and ansible_distribution_version=="14.04" }
